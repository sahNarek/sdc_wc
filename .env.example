# Copy to .env and fill in values. Never commit .env to git.
#
# Optional: StatsBomb / SDC API credentials (for future automated JSON download)
STATSBOMB_USERNAME=
STATSBOMB_PASSWORD=

# Pipeline defaults (used by Docker entrypoint when implemented)
SDC_PRIMARY_MATCH_ID=4036731
SDC_REPORT_FORMAT=both
